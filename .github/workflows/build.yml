name: build

on: [push]

jobs:
  build:
    name: Create Release

    runs-on: ${{ matrix.os }}

    env:
      toolname: sfc-comp

    strategy:
      matrix:
        include:
          - os: windows-latest
            generator: "MinGW Makefiles"
            file_suffix: windows.7z

          - os: ubuntu-latest
            generator: "Unix Makefiles"
            file_suffix: linux.tar.xz

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v1

      - name: Build
        run: |
          mkdir build
          cd build
          cmake .. -G "${{ matrix.generator }}" -DCMAKE_BUILD_TYPE=Release
          cmake --build .

      - name: Create an archive
        run: |
          mkdir ${{ env.toolname }}
          mv build/bin doc LICENSE README.md ${{ env.toolname }}
          if [ "$RUNNER_OS" == "Linux" ]; then
            tar cf - ${{ env.toolname }} | xz -z > ${{ env.toolname }}-${{ matrix.file_suffix }}
          elif [ "$RUNNER_OS" == "Windows" ]; then
            7z a ${{ env.toolname }}-${{ matrix.file_suffix }} ${{ env.toolname }}
          else
            echo "Not a supported os: $RUNNER_OS"
            exit 1
          fi

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./${{ env.toolname }}-${{ matrix.file_suffix }}
          asset_name: ${{ env.toolname }}-${{ matrix.file_suffix }}
          asset_content_type: application/octet-stream
